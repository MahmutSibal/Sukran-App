import 'package:flutter/material.dart';

class AppColors {
  // Şükran App brand: deep emerald green + matte gold.
  static const Color primary = Color(0xFF103D24); // brand deep green
  static const Color accent = Color(0xFFDDA15E); // brand gold
  static const Color background = Color(0xFFF6F3E9); // warm ivory
  static const Color dark = Color(0xFF08210F); // forest near-black
  static const Color surface = Color(0xFFFFFFFF);
  static const Color softPeach = Color(0xFFE3EDE2); // soft sage tint
  static const Color warmSand = Color(0xFFEAE2CE); // gold-sand divider
  static const Color muted = Color(0xFF6E7E6F);
  static const Color success = Color(0xFF2E9E5B);
  static const Color warning = Color(0xFFDDA15E);
  static const Color info = Color(0xFF3F7D5E);
}