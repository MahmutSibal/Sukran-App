import 'package:flutter/material.dart';

class AppColors {
  // Şükran App — siyah-beyaz (monokrom) palet.
  static const Color primary = Color(0xFF141414); // koyu gri (birincil)
  static const Color accent = Color(0xFFFAFAFA); // beyaz vurgu
  static const Color background = Color(0xFFFFFFFF); // beyaz zemin
  static const Color dark = Color(0xFF000000); // saf siyah
  static const Color surface = Color(0xFFFFFFFF);
  static const Color softPeach = Color(0xFFEDEDED); // açık gri tint
  static const Color warmSand = Color(0xFFE0E0E0); // gri divider
  static const Color muted = Color(0xFF6B6B6B);
  static const Color success = Color(0xFF555555);
  static const Color warning = Color(0xFFFAFAFA);
  static const Color info = Color(0xFF666666);
}